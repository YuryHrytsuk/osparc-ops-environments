# CI-only stub for Trivy chart scanning.

repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: security-hardening
    chart: ./security-hardening
    namespace: kube-system
    values:
      - security-hardening/values.yaml

  - name: renovate-canary
    chart: bitnami/nginx
    version: 15.0.0
    namespace: default
